project_id = "nqztdxdghjkgexqmcmgj"

[functions.sync-orders]
verify_jwt = false  # Allow invoking without a JWT (for cron)

# Schedule the cron job (e.g., every 05 minutes)
# Format: Minute Hour Day Month DayOfWeek
# Schedule must be configured in Dashboard or SQL
# schedule = "*/5 * * * *"
